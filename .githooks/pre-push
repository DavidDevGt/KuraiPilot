#!/usr/bin/env bash
# Gate local antes de push: lo mismo que exige CI, más rápido fallar acá.
# Instalación: make hooks   (git config core.hooksPath .githooks)
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
echo "pre-push: make check"
make check
